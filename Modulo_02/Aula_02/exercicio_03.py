"""3. Contando Elementos: Crie uma tupla numeros = (1, 2, 3, 2, 4, 2). Use o método .count()
para descobrir quantas vezes o número 2 aparece"""

numeros = (1, 2, 3, 2, 4, 2)
print(numeros.count(2))
